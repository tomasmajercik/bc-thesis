train_ratio: 0.8
batch_size: 2
num_epochs: 10
optimizer: "adam"
learning_rate: 1e-4
debug: True

wandb:
  use_wandb: true
  project: "bc-thesis"
  run_name: null # auto
  log_interval: 1 # every N epoch