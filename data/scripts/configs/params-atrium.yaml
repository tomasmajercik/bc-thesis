# atrium dataset preprocessing parameters
for_input:
  local_scale: 3.0 
  context_scale: 1.5 
  traj_sampling_method: "linear" 
  past_traj_steps: 10

for_groundtruth:
  save_as: "Atrium-Balanced"
  future_traj_steps: 15           
  traj_sampling_method: "gaussian" 